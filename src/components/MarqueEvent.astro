---
const { header, desc, className } = Astro.props;
---

<div
  class={`bg-gray-300 p-4 text-black shadow-[20px_20px] shadow-secondary ${className}`}
>
  <h3 class="mb-1 text-2xl font-bold">{header}</h3>
  <div class="text-xl font-normal uppercase">{desc}</div>
</div>
